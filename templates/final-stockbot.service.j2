[Unit]
Description=Final Stockbot
After=network.target

[Service]
User={{ service_user_name }}
WorkingDirectory=/var/lib/{{ service_user_name }}
ExecStart=/var/lib/{{ service_user_name }}/the_final_stockbot
Restart=always

[Install]
WantedBy=multi-user.target